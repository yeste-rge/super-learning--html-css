1.语法直接写在 html 标签里面
eg. 如果我想要变更 h2 文字标题的色彩 我们可以输入一个 style 属性(可使用颜色的 RGB 值或 16进位色码) 
2.这个方式是把CSS语法统一到 html 标签里面 并且前后用 style 标签包覆起来
eg. 如果我们想要修改 h2 的标题色彩 只要清楚指明 html 的标签名称 输入一组大挂号 再把 css 的语法写在里面 
    就可以一次影响网页中所有的 h2 标题
    /譬如去修改 P 标签的文字色彩 而如果用  font-size 的语法 则可变更内文字体的大小